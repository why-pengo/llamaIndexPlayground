[pytest]
# Silently ignore the DeprecationWarning about datetime.utcfromtimestamp raised
# by tqdm on certain Python versions. This is a dependency deprecation and
# harmless for our tests.
filterwarnings =
    ignore:.*utcfromtimestamp.*:DeprecationWarning
